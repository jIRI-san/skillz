# Autopilot transient artifacts (never committed)
.autopilot-halt
.autopilot-finalize-needed

# Plugin install runtime state (receipts, tmp staging) — never committed
.github/.skalary/

# Autopilot local runtime/host configuration (never committed)
.autopilot.json
.autopilot.host.json

# Eval harness local outputs/config
.eval-report.json
.eval.config.json
.eval-artifacts/
tests/evals/output/

# Pester CI test output
testResults.xml
